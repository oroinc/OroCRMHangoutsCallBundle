{% set cssClass = 'btn icons-holder-text' %}

{% if phone is defined %}
    {% set invites = [{
        name: entity is not empty ? entity|escape('html') : '',
        id: phone|escape('html'),
        invite_type: 'PHONE'
    }] %}
{% elseif email is defined %}
    {% set invites = [{
        name: entity is not empty ? entity|escape('html') : '',
        id: email|escape('html'),
        invite_type: 'EMAIL',
    }] %}
{% endif %}

{% set hangoutOptions = {
    invites: invites is defined ? invites : []
} %}

{% include "OroCRMHangoutsCallBundle:Call:inviteHangoutLink.html.twig" %}
