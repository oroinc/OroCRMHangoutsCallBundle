{% set cssClass = 'btn icons-holder-text' %}

{% if phone is defined %}
    {% set invites = [{
        name: '' ~ entity,
        id: phone,
        invite_type: 'PHONE'
    }] %}
{% elseif email is defined %}
    {% set invites = [{
        name: '' ~ entity,
        id: email,
        invite_type: 'EMAIL',
    }] %}
{% endif %}

{% set hangoutOptions = {
    invites: invites is defined ? invites : []
} %}

{% include "OroCRMHangoutsCallBundle:Call:hangoutsLink.html.twig" %}
