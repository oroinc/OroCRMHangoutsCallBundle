{% import 'OroUIBundle::macros.html.twig' as UI %}

{% set options = {
    class: cssClass is defined ? cssClass : '',
    aCss: 'no-hash',
    iCss: 'icon-hangouts',
    label: 'orocrm.hangoutscall.call.hangouts_call'|trans,
    pageComponent: {
        module: 'orocrmhangoutscall/js/app/components/invite-hangout-component',
        options: {
            modalOptions: {
                hangoutOptions: hangoutOptions is defined ? hangoutOptions : {}
            }
        }|merge(extraComponentOptions is defined ? extraComponentOptions : {})
    }
} %}

{% block action_controll %}
    {{ UI.clientLink(options) }}
{% endblock %}
